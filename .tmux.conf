# ==========================================
# CONFIGURACIÓN BÁSICA DE TMUX
# ==========================================

# Habilitar soporte de colores verdaderos (esencial para el tema de Neovim)
set-option -sa terminal-overrides ",xterm*:Tc"

# Habilitar el uso del ratón (cambiar tamaño de paneles, hacer clic)
set -g mouse on

# Empezar a contar las ventanas y paneles desde 1 en lugar de 0
set -g base-index 1
set -g pane-base-index 1

# ==========================================
# NAVEGACIÓN TRANSPARENTE CON NEOVIM
# ==========================================
# Este script detecta si estás dentro de Vim/Neovim y manda las pulsaciones
# correctamente. Si no estás en Vim, cambia de panel en Tmux.

is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"

bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'

tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"
